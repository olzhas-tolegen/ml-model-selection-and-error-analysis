# Repro Notes

Run `bash scripts/run_all.sh` from the project root after placing `covtype.data` in `data/raw/covertype/`.

Generated outputs are intentionally excluded from source control unless they are needed for the report. remove exploratory files/data with `bash scripts/clean_outputs.sh`.
